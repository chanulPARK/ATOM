insertApproval=INSERT INTO ELECTRONICAPPROVAL VALUES(?,?,?,?,?,?,DEFAULT)
insertMaterialLine=INSERT INTO MATERIALLINE VALUES(MATERIALLINE_SEQ.NEXTVAL,?,?,?,?)
selectWaitApproval=SELECT * FROM (SELECT ROWNUM AS RNUM, EA.* FROM(SELECT EA.*, EMP_NAME FROM ELECTRONICAPPROVAL EA JOIN EMPLOYEE E ON(EA.EMP_ID = E.EMP_ID) WHERE EA.EMP_ID = ? AND DRAFT_STATE = ?) EA) WHERE RNUM BETWEEN ? AND ?
selectApproavalCount=SELECT COUNT(*) AS APPROVALCNT FROM ELECTRONICAPPROVAL WHERE EMP_ID=? AND DRAFT_STATE=?